# 延期工作

## Story 1.2 评审延期项

- **未保存内容保护：** 新建、打开规划包及跨页面离开前增加脏数据检测与确认流程，并明确导航失败时当前工作区的保留规则。
- **运行期间操作锁定：** 算法预处理和运行期间禁止新建、打开或切换规划包，避免前端状态与后端计算输入不一致。
- **多标签页与多用户隔离：** 当前启发式后端共用固定输入输出文件。需要重新设计 API 和后端任务隔离机制，为每次运行分配独立工作目录或任务标识后再支持并发。
